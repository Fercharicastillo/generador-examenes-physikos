services:
  api:
    build:
      context: .
    ports:
      - "8000:8000"
    environment:
      CORS_ALLOWED_ORIGINS: "http://localhost:5173,http://127.0.0.1:5173,https://fercharicastillo.github.io"
      PLUMBER_DEBUG: "false"
      PLUMBER_SWAGGER: "true"
